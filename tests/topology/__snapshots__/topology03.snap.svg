<svg width="640" height="640" viewBox="0 0 640 640" xmlns="http://www.w3.org/2000/svg"><rect width="100%" height="100%" fill="white"/><g><polyline data-points="0,0 0,0 0,0" data-type="line" data-label="" points="320,320 320,320 320,320" fill="none" stroke="rgba(100, 100, 100, 0.3)" stroke-width="1px"/></g><g><rect data-type="rect" data-label="" data-x="0" data-y="0" x="193.54838709677418" y="256.7741935483871" width="252.90322580645167" height="126.45161290322585" fill="rgba(255, 200, 100, 0.5)" stroke="black" stroke-width="0.0027678571428571427"/></g><g><rect data-type="rect" data-label="" data-x="0" data-y="0" x="40" y="283.8709677419355" width="560" height="72.25806451612902" fill="rgba(100, 200, 100, 0.5)" stroke="black" stroke-width="0.0027678571428571427"/></g><circle data-type="circle" data-label="" data-x="0" data-y="0" cx="320" cy="320" r="18.06451612903226" fill="rgba(128, 128, 128, 0.5)" stroke="black" stroke-width="0.0027678571428571427"/><g id="crosshair" style="display: none"><line id="crosshair-h" y1="0" y2="640" stroke="#666" stroke-width="0.5"/><line id="crosshair-v" x1="0" x2="640" stroke="#666" stroke-width="0.5"/><text id="coordinates" font-family="monospace" font-size="12" fill="#666"></text></g><script><![CDATA[
              document.currentScript.parentElement.addEventListener('mousemove', (e) => {
                const svg = e.currentTarget;
                const rect = svg.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                const crosshair = svg.getElementById('crosshair');
                const h = svg.getElementById('crosshair-h');
                const v = svg.getElementById('crosshair-v');
                const coords = svg.getElementById('coordinates');
                
                crosshair.style.display = 'block';
                h.setAttribute('x1', '0');
                h.setAttribute('x2', '640');
                h.setAttribute('y1', y);
                h.setAttribute('y2', y);
                v.setAttribute('x1', x);
                v.setAttribute('x2', x);
                v.setAttribute('y1', '0');
                v.setAttribute('y2', '640');

                // Calculate real coordinates using inverse transformation
                const matrix = {"a":361.2903225806452,"c":0,"e":320,"b":0,"d":-361.2903225806452,"f":320};
                // Manually invert and apply the affine transform
                // Since we only use translate and scale, we can directly compute:
                // x' = (x - tx) / sx
                // y' = (y - ty) / sy
                const sx = matrix.a;
                const sy = matrix.d;
                const tx = matrix.e; 
                const ty = matrix.f;
                const realPoint = {
                  x: (x - tx) / sx,
                  y: (y - ty) / sy // Flip y back since we used negative scale
                }
                
                coords.textContent = `(${realPoint.x.toFixed(2)}, ${realPoint.y.toFixed(2)})`;
                coords.setAttribute('x', (x + 5).toString());
                coords.setAttribute('y', (y - 5).toString());
              });
              document.currentScript.parentElement.addEventListener('mouseleave', () => {
                document.currentScript.parentElement.getElementById('crosshair').style.display = 'none';
              });
            ]]></script></svg>